import React, { useState } from "react";
import axios from "axios";
import { useAuth } from "../AuthContext";
import { useNavigate } from "react-router-dom";
import "../styles/Login.css";
import "../styles/Modal.css";

const Login = () => {
  const { login } = useAuth();
  const navigate = useNavigate();

  // States
  const [modalType, setModalType] = useState(""); // "login" or "signup"
  const [error, setError] = useState("");
  const [role, setRole] = useState("User");
  const [userName, setUserName] = useState("");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [phoneNumber, setPhoneNumber] = useState("");
  const [location, setLocation] = useState("");

  // Handle Login
  const handleLogin = async () => {
    try {
      const endpoint =
        role === "User"
          ? "https://localhost:7283/api/Users/login"
          : "https://localhost:7283/api/Admin/login";

      const response = await axios.post(endpoint, { email, password });
      const { token, role: userRole, userId } = response.data;

      localStorage.setItem("authToken", token);
      localStorage.setItem("role", userRole);
      localStorage.setItem("userId", userId);

      login(token, userRole, userId);
      alert("Login successful!");
      setModalType("");
      navigate("/dashboard");
    } catch {
      setError("Login failed. Please check your credentials.");
    }
  };

  // Handle Signup
  const handleSignup = async () => {
    try {
      const endpoint =
        role === "User"
          ? "https://localhost:7283/api/Users/register"
          : "https://localhost:7283/api/Admin/register";

      const dataToSend = {
        userName,
        email,
        password,
        role,
        phoneNumber: parseInt(phoneNumber, 10),
        location,
      };

      await axios.post(endpoint, dataToSend);
      alert("Signup successful! Please login.");
      setModalType("");
    } catch {
      setError("Signup failed. Please try again.");
    }
  };

  return (
    <div className="login-landing">
      <div className="welcome-box">
        <h1>
          Welcome to <span>SIMBA Events</span>
        </h1>
        <p>
          Manage, explore, and create amazing experiences with a single platform
          for users and organisers.
        </p>

        <div className="landing-buttons">
          <button className="btn btn-user" onClick={() => setModalType("login")}>
            Login
          </button>
          <button className="btn btn-admin" onClick={() => setModalType("signup")}>
            Signup
          </button>
        </div>
      </div>

      {/* LOGIN MODAL */}
      {modalType === "login" && (
        <div className="modal-overlay">
          <div className="modal-card">
            <button className="close-btn" onClick={() => setModalType("")}>
              ×
            </button>
            <h2>Login</h2>
            {error && <div className="error-box">{error}</div>}

            <form
              onSubmit={(e) => {
                e.preventDefault();
                handleLogin();
              }}
            >
              <select value={role} onChange={(e) => setRole(e.target.value)} required>
                <option value="User">User</option>
                <option value="Organiser">Organiser</option>
              </select>
              <input
                type="email"
                placeholder="Email address"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
              />
              <input
                type="password"
                placeholder="Password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
              />
              <button type="submit">Login</button>
            </form>
          </div>
        </div>
      )}

      {/* SIGNUP MODAL */}
      {modalType === "signup" && (
        <div className="modal-overlay">
          <div className="modal-card">
            <button className="close-btn" onClick={() => setModalType("")}>
              ×
            </button>
            <h2>Signup</h2>
            {error && <div className="error-box">{error}</div>}

            <form
              onSubmit={(e) => {
                e.preventDefault();
                handleSignup();
              }}
            >
              <select value={role} onChange={(e) => setRole(e.target.value)} required>
                <option value="User">User</option>
                <option value="Organiser">Organiser</option>
              </select>
              <input
                type="text"
                placeholder="Full Name"
                value={userName}
                onChange={(e) => setUserName(e.target.value)}
                required
              />
              <input
                type="email"
                placeholder="Email address"
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
              />
              <input
                type="password"
                placeholder="Password"
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
              />
              <input
                type="tel"
                placeholder="Phone Number"
                value={phoneNumber}
                onChange={(e) => setPhoneNumber(e.target.value)}
                required
              />
              <select
                value={location}
                onChange={(e) => setLocation(e.target.value)}
                required
              >
                <option value="">-- Select Your City --</option>
                <option value="Amaravati">Amaravati (Andhra Pradesh)</option>
                <option value="Itanagar">Itanagar (Arunachal Pradesh)</option>
                <option value="Patna">Patna (Bihar)</option>
                <option value="Mumbai">Mumbai (Maharashtra)</option>
                <option value="Hyderabad">Hyderabad (Telangana)</option>
                <option value="Chennai">Chennai (Tamil Nadu)</option>
                <option value="Kolkata">Kolkata (West Bengal)</option>
                <option value="Delhi">New Delhi (NCT of Delhi)</option>
                <option value="Bhopal">Bhopal (Madhya Pradesh)</option>
                <option value="Lucknow">Lucknow (Uttar Pradesh)</option>
                <option value="Puducherry">Puducherry (UT)</option>
              </select>
              <button type="submit">Signup</button>
            </form>
          </div>
        </div>
      )}
    </div>
  );
};

export default Login;
-----------------------

:root {
  --primary-color: #7a4f3e;
  --accent-color: #ffa726;
  --secondary-color: #88024f;
}

/* ===== Overlay ===== */
.modal-overlay {
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.55);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 9999;
  animation: fadeIn 0.3s ease-in-out;
}

/* ===== Modal Card ===== */
.modal-card {
  position: relative;
  background: #ffffff;
  border-radius: 14px;
  padding: 2.5rem;
  width: 400px;
  box-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
  animation: slideUp 0.3s ease-out;
  text-align: center;
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ===== Close Button (Floating) ===== */
.close-btn {
  position: absolute;
  top: 10px;
  right: 12px;
  font-size: 1.7rem;
  color: #666;
  background: transparent;
  border: none;
  cursor: pointer;
  transition: all 0.25s ease;
  z-index: 10;
}

.close-btn:hover {
  color: var(--accent-color);
  transform: rotate(90deg) scale(1.2);
}

/* ===== Heading ===== */
.modal-card h2 {
  color: var(--primary-color);
  margin-top: 10px;
  margin-bottom: 1.2rem;
}

/* ===== Form Elements ===== */
.modal-card input,
.modal-card select {
  width: 100%;
  padding: 10px 12px;
  margin-bottom: 1rem;
  border: 1px solid #ccc;
  border-radius: 6px;
  font-size: 1rem;
  transition: all 0.3s;
}

.modal-card input:focus,
.modal-card select:focus {
  border-color: var(--accent-color);
  box-shadow: 0 0 5px rgba(255, 167, 38, 0.4);
}

/* ===== Button ===== */
.modal-card button {
  width: 100%;
  padding: 12px;
  border-radius: 6px;
  border: none;
  color: white;
  font-size: 1rem;
  cursor: pointer;
  background: var(--primary-color);
  transition: all 0.3s ease;
}

.modal-card button:hover {
  background: var(--accent-color);
  transform: scale(1.05);
}

/* ===== Error Box ===== */
.error-box {
  color: var(--secondary-color);
  background-color: rgba(255, 0, 0, 0.05);
  border-left: 3px solid var(--secondary-color);
  padding: 8px 10px;
  margin-bottom: 1rem;
  border-radius: 4px;
  font-size: 0.9rem;
}

/* ===== Responsive ===== */
@media (max-width: 500px) {
  .modal-card {
    width: 90%;
    padding: 2rem;
  }
}
